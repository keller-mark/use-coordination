---
id: provider-components
title: Provider components
slug: /provider-components
---

## `<CmvProvider/>`

### Required props

#### `config`

- Type: `object`

#### `children`

- Type: JSX

### Optional props

#### `onConfigChange`

- Type: `function`

A callback function for config updates.

#### `validateOnConfigChange`

- Type: `boolean`

Whether to validate using the `validater` function when publishing changes.
Use for debugging purposes only, as this has a performance impact.

#### `validater`

- Type: `function`

Validation function to run when `validateOnConfigChange` is true.

#### `initializer`

- Type: `function`

Initialization function to run during the creation of the Zustand store.

#### `onCreateStore`

- Type: `function`

A function to enable access to the Zustand store on its creation.
This return value will be passed to the Zustand store creation function.
The function will be passed the `set` parameter from `create`.

## `<ZodCmvProvider/>`

Inherits all props except `validater` from `CmvProvider`.

### Required props

#### `coordinationTypes`

- Type: `Record<string, z.ZodTypeAny>`

Mapping from coordination type names to Zod schemas.

### Optional props

#### `validateConfig`

- Type: `boolean`

Should the config be validated against the Zod schema?

## `<ZodErrorBoundary/>`

Wrap `<ZodCmvProvider/>` in this error boundary to catch validation errors.
Alternatively, implement your own [ErrorBoundary](https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary).

