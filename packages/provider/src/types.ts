export type CmvProviderProps = {
    coordinationSpace: any; // Record<string, any>;
    viewCoordination: any; // Record<string, any>;
    initStrategy: 'auto';
    coordinationTypes: any[]; // PluginCoordinationType[];
};